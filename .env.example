# Project Chimera — environment example. Copy to .env and set values.
# Do not commit .env (secrets). See constitution: no hardcoded secrets.

REDIS_URL=redis://localhost:6379/0
# DATABASE_URL=postgresql://user:pass@localhost:5432/chimera
