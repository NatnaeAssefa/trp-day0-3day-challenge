"""
Download YouTube skill — stub. Contract: skills/skill_download_youtube/README.md.
Implement to return dict with success, local_path, blob_ref, error.
"""


def download_youtube(video_url: str, format: str = "video") -> dict:
    """Stub: returns wrong shape so tests fail until implementation fulfils contract."""
    # Deliberately wrong structure so test_skills_interface.py fails (TDD "empty slot").
    return {"status": "not_implemented"}
